name: Keep Render Backend Alive

on:
  schedule:
    # Runs every 12 minutes to prevent Render free tier from sleeping (which happens after 15 mins of inactivity)
    - cron: "*/12 * * * *"
  workflow_dispatch: # Allows manual trigger from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend API
        run: |
          # If your backend URL is different, update it here or use a GitHub Secret
          URL=${BACKEND_URL:-"https://moneypal.onrender.com/"}
          echo "Pinging backend at $URL..."

          # Render cold starts can take 30-50s, so allow enough time to wake it,
          # and never let a slow/failed curl (set -e) kill this step.
          response=$(curl -s -o /dev/null -w "%{http_code}" --max-time 60 "$URL" || echo "000")

          if [ "$response" -eq 200 ]; then
            echo "Successfully pinged backend. Status: $response"
          else
            echo "Backend did not return 200 (status: $response). This is fine if it was just waking up."
          fi
          exit 0
        env:
          BACKEND_URL: ${{ secrets.RENDER_BACKEND_URL }}
